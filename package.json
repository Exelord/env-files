{
  "name": "env-files",
  "version": "0.2.0",
  "description": "Load app config using environments files",
  "main": "index.js",
  "repository": "https://github.com/Exelord/env-files",
  "author": "Exelord",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "release": "release-it"
  },
  "release-it": {
    "github": {
      "release": true,
      "releaseName": "v${version}"
    },
    "git": {
      "tagName": "v${version}",
      "commitMessage": "Release v${version}",
      "pushArgs": "--follow-tags --no-verify"
    }
  },
  "dependencies": {
    "lodash.merge": "^4.6.2"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "release-it": "^15.6.0"
  }
}
